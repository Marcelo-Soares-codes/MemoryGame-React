import { Component } from 'react';
import { Card } from './index';

export default {
  component: Card,
};

export const Template = () => {
  return (
    <div>
      <Card></Card>
    </div>
  );
};
